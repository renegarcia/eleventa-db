
```sql
CREATE TABLE DEPARTAMENTOS (ID TLLAVE NOT NULL,
        NOMBRE VARCHAR(255) NOT NULL,
        PORCENTAJE_IMPUESTO TSMALLINT DEFAULT 0.0,
        ACTIVO TBOOLEANO DEFAULT 1,
CONSTRAINT PK_DEPARTAMENTOS PRIMARY KEY (ID),
CONSTRAINT UNQ1_DEPARTAMENTOS UNIQUE (NOMBRE, ACTIVO));
```