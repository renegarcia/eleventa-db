---
id: 3kp7ohra9xepc9a167c8ote
title: OBTEDOCUENTA
desc: null
updated: 1684912753
created: 1684912753
---


```sql
CREATE OR ALTER PROCEDURE OBTEDOCUENTA (PAR_CLIENTE_ID INTEGER)
RETURNS (FECHA DATE,
MONTO FLOAT,
TIPO VARCHAR(50) CHARACTER SET NONE)
AS
BEGIN SUSPEND; END ^
CREATE OR ALTER PROCEDURE OBTEDOCUENTA2 (PAR_CLIENTE_ID INTEGER)
RETURNS (FECHA TIMESTAMP,
MONTO NUMERIC(15, 4),
TIPO VARCHAR(50) CHARACTER SET NONE)
AS
BEGIN SUSPEND; END ^
CREATE OR ALTER PROCEDURE OBTEDOCUENTA3 (PAR_CLIENTE_ID INTEGER)
RETURNS (FECHA TIMESTAMP,
MONTO NUMERIC(15, 4),
TIPO VARCHAR(50) CHARACTER SET NONE)
AS
BEGIN SUSPEND; END ^
CREATE OR ALTER PROCEDURE POS (SUBSTR VARCHAR(255) CHARACTER SET NONE,
STR VARCHAR(255) CHARACTER SET NONE)
RETURNS (POS INTEGER)
AS
BEGIN SUSPEND; END ^
CREATE OR ALTER PROCEDURE YEARWEEK (D DATE)
RETURNS (WEEK_NO VARCHAR(8) CHARACTER SET NONE)
AS
BEGIN SUSPEND; END ^
CREATE OR ALTER PROCEDURE YEAR_WEEK (IDENTRADA INTEGER,
D DATE)
RETURNS (ID INTEGER,
WEEK_NO VARCHAR(8) CHARACTER SET NONE)
AS
BEGIN SUSPEND; END ^

SET TERM ;
```
